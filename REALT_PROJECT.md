# Realt Assistant — Документация проекта

## 🎯 Что это

**Realt Assistant** — Telegram-бот для риэлторов. Персональный AI-ассистент для работы с базой ЖК.

**Концепция v2**: Загрузка данных из YGroup API → структурированное хранение → гибридные запросы (SQL + RAG).

**Для кого**: Индивидуальные риэлторы, работающие "на ходу" с разными застройщиками.

---

## 📍 Инфраструктура

| Компонент | Значение |
|-----------|----------|
| Сервер | `72.56.64.91:2222` (SSH) |
| Путь | `/opt/realt-assistant` |
| Telegram бот | `@RealtAssistentAI_bot` |
| GitHub | https://github.com/semiekhin/realt-assistant |
| Python | 3.12+ |
| База | SQLite (`data/assistant.db`) |
| Векторная БД | ChromaDB (`data/chroma/`) |
| LLM | OpenAI gpt-4o |
| Внешний API | YGroup API (`api-ru.ygroup.ru/v2/`) |

**Другие сервисы на сервере (НЕ ТРОГАТЬ)**:
- `/opt/bot` — RIZALTA PROD (порт 8000)
- `/opt/bot-dev` — RIZALTA DEV (порт 8002)
- `/opt/sofia-claude`, `/opt/sofia-gpt`

---

## 🚀 Запуск и управление
```bash
# Запуск
cd /opt/realt-assistant
/opt/realt-assistant/venv/bin/python -u run_polling.py > bot.log 2>&1 &

# Проверка статуса
ps aux | grep "realt-assistant" | grep -v grep

# Остановка
pkill -f "realt-assistant.*run_polling"

# Логи
tail -f /opt/realt-assistant/bot.log

# Перезапуск
pkill -f "realt-assistant.*run_polling" 2>/dev/null; sleep 1 && cd /opt/realt-assistant && /opt/realt-assistant/venv/bin/python -u run_polling.py > bot.log 2>&1 & sleep 2 && tail -5 bot.log
```

---

## 📁 Структура проекта
```
/opt/realt-assistant/
├── app.py                     # Главный роутинг + универсальный handler
├── run_polling.py             # Запуск в polling режиме
├── config.py                  # Конфигурация
├── requirements.txt
├── .env                       # Секреты (не в git)
│
├── docs/
│   └── RAG_CORE_SPEC.md       # ТЗ на RAG-ядро
│
├── bot/
│   ├── states.py              # FSM состояния (только для загрузки файлов)
│   └── handlers/
│       ├── start.py           # /start, меню
│       ├── add_property.py    # Добавление ЖК + индексация в RAG
│       ├── query.py           # Поиск через RAG, просмотр ЖК
│       ├── kp.py              # Генерация КП (старый)
│       └── calculators.py     # Калькуляторы + привязка к ЖК
│
├── services/
│   ├── telegram.py            # Telegram API
│   ├── llm.py                 # OpenAI API + промпты
│   ├── rag.py                 # RAG engine (ChromaDB + embeddings)
│   ├── html_to_pdf.py         # HTML → PDF через wkhtmltopdf
│   ├── ygroup.py              # **ПЛАНИРУЕТСЯ** — YGroup API
│   ├── parser.py              # Старый парсер (не используется)
│   ├── parser_v2.py           # Vision-first парсер (PDF → GPT-4o)
│   ├── calculators.py         # Расчёты рассрочки/ипотеки/ROI
│   ├── content_composer.py    # AI-генерация контента (старый)
│   ├── kp_generator_v2.py     # PDF рендеринг (старый)
│   ├── pdf_styles.py          # 6 стилей оформления (старый)
│   └── fonts/                 # DejaVuSans для PDF
│
├── db/
│   ├── database.py            # SQLite операции + chat_history
│   └── models.py              # Dataclass модели
│
└── data/
    ├── assistant.db           # SQLite база
    ├── chroma/                # ChromaDB векторная база
    ├── uploads/               # Загруженные файлы
    └── kp_output/             # Сгенерированные PDF
```

---

## 🏗 Новая архитектура (v0.6.0 — планируется)

### Источник данных: YGroup API
```
YGroup API (api-ru.ygroup.ru/v2/)
    │
    ├── GET /facilities — список ЖК
    │   └── name, city, developer, commission, description...
    │
    ├── GET /clusters?facility_id={id} — корпуса
    │   └── name, floors, commissioning_date...
    │
    └── GET /lots?cluster_id={id} — квартиры
        └── name, price, area, floor, rooms, layout_image...
```

### Новый флоу добавления ЖК
```
1. Риэлтор: /add → "ЖК Белые Ночи"
2. Бот ищет в YGroup API по названию
3. Находит → показывает карточку для подтверждения
4. Риэлтор подтверждает → бот загружает все данные
5. Бот спрашивает кастомные данные (УТП, рассрочка)
6. Сохраняет в БД → готово к работе
```

### Гибридная обработка запросов

| Тип запроса | Пример | Обработка |
|-------------|--------|-----------|
| Точный/числовой | "от 15 до 19 млн" | SQL → таблица units |
| Семантический | "чем лучше конкурентов?" | RAG → ChromaDB |
| Смешанный | "недорогие у моря" | SQL (цена) + RAG (море) |

### Таблицы БД (планируется)
```sql
-- Квартиры/юниты (NEW)
units:
  id, property_id,
  ygroup_id,          -- ID в YGroup
  unit_number,        -- "203"
  area,               -- 45.5
  price,              -- 16500000
  price_per_sqm,      -- 362637
  floor,              -- 8
  rooms,              -- 1 (0 для студии)
  decoration_type,    -- тип отделки
  layout_image,       -- URL планировки
  status,             -- available/sold
  created_at, updated_at

-- Кастомные данные риэлтора (NEW)
property_custom:
  id, property_id,
  utp,                -- "Лучший вид на море"
  installment_info,   -- "50/50 до сдачи без удорожания"
  custom_commission,  -- если отличается от YGroup
  notes,              -- заметки
  created_at, updated_at
```

---

## ✅ Реализованный функционал (v0.5.1)

### Универсальный handler
- **Любое сообщение → RAG → LLM** — без жёстких меню
- **История чата** — таблица chat_history, контекст 6 сообщений
- **Автодетекция действий** — LLM возвращает JSON для калькуляторов/КП
- **FSM только для загрузки файлов**

### Генерация КП через HTML
- **LLM → HTML → PDF** — адаптивная генерация
- **wkhtmltopdf** — рендеринг HTML в PDF
- **Автовыбор ЖК** — по имени из запроса

### RAG-ядро
- **ChromaDB** — векторное хранилище чанков
- **Эмбеддинги** — OpenAI text-embedding-3-small
- **Семантический поиск** — limit=50

### Калькуляторы
- **Рассрочка, ипотека (4 программы), ROI**
- **Вызов через LLM** — natural language

---

## 🗄 База данных (текущая)

### SQLite
```sql
-- users
id, telegram_id, username, first_name, last_name, state, state_data, created_at

-- properties  
id, user_id, name, address, city, developer, completion_date,
price_min, price_max, price_per_sqm_min, price_per_sqm_max,
apartment_types, area_min, area_max,
payment_options, installment_terms, mortgage_info,
installment_min_pv, installment_max_months, installment_markup,
distance_to_sea, territory_area, hotel_operator,
commission, description, features, raw_data, 
created_at, updated_at

-- property_files
id, property_id, user_id, file_id, file_name, file_type, file_path, extracted_text, created_at

-- chat_history
id, user_id, role, content, created_at
```

### ChromaDB
```
Коллекция: user_{telegram_id}
- documents: текст чанков
- metadatas: {property_id, property_name, file_name, chunk_index}
- embeddings: векторы text-embedding-3-small
```

---

## 📋 История версий

### v0.6.0 (планируется)
- **Интеграция YGroup API** — загрузка ЖК из внешнего API
- **Таблица units** — структурированные данные о квартирах
- **Гибридные запросы** — SQL + RAG
- **Кастомные поля** — УТП, рассрочка от риэлтора

### v0.5.1 (08.01.2026 вечер)
- **LLM → HTML → PDF** — адаптивная генерация КП
- services/html_to_pdf.py — конвертер через wkhtmltopdf
- gpt-4o — точная работа с числами
- limit=50 — полнота поиска квартир

### v0.5.0 (08.01.2026 вечер)
- **Универсальный handler** — любое сообщение → RAG → LLM
- Таблица chat_history — контекст диалога
- UNIVERSAL_PROMPT — детекция действий

### v0.4.2 (08.01.2026 ночь)
- **RAG-ядро** — ChromaDB + text-embedding-3-small
- services/rag.py — chunking, embeddings, search
- ~170 чанков проиндексированы

### v0.4.0 (07.01.2026 вечер)
- **Vision-first парсер** — PDF как изображения через GPT-4o

### v0.3.0 (07.01.2026)
- Калькуляторы: рассрочка, ипотека, ROI
- Content Composer + 6 стилей PDF
- GitHub репозиторий
