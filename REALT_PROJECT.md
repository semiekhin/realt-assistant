# Realt Assistant — Документация проекта

## 🎯 Что это

**Realt Assistant** — Telegram-бот для риэлторов. Персональный AI-ассистент для работы с базой ЖК.

**Концепция**: Риэлтор загружает документы по ЖК (прайсы, презентации, планировки) → бот парсит и структурирует → риэлтор быстро отвечает клиентам, ищет варианты, генерирует КП.

**Для кого**: Индивидуальные риэлторы, работающие "на ходу" с разными застройщиками.

---

## 📍 Инфраструктура

| Компонент | Значение |
|-----------|----------|
| Сервер | `72.56.64.91:2222` (SSH) |
| Путь | `/opt/realt-assistant` |
| Telegram бот | `@RealtAssistentAI_bot` |
| GitHub | https://github.com/semiekhin/realt-assistant |
| Python | 3.12+ |
| База | SQLite (`data/assistant.db`) |

**Другие сервисы на сервере (НЕ ТРОГАТЬ)**:
- `/opt/bot` — RIZALTA PROD (порт 8000)
- `/opt/bot-dev` — RIZALTA DEV (порт 8002)
- `/opt/sofia-claude`, `/opt/sofia-gpt`

---

## 🚀 Запуск и управление
```bash
# Запуск
cd /opt/realt-assistant
/opt/realt-assistant/venv/bin/python -u run_polling.py > bot.log 2>&1 &

# Проверка статуса
ps aux | grep "realt-assistant" | grep -v grep

# Остановка
pkill -f "realt-assistant.*run_polling"

# Логи
tail -f /opt/realt-assistant/bot.log

# Очистка кэша
find /opt/realt-assistant -name "__pycache__" -exec rm -rf {} + 2>/dev/null
```

---

## 📁 Структура проекта
```
/opt/realt-assistant/
├── app.py                     # FastAPI webhook + роутинг
├── run_polling.py             # Запуск в polling режиме
├── config.py                  # Конфигурация
├── requirements.txt
├── .env                       # Секреты (не в git)
│
├── bot/
│   ├── states.py              # FSM состояния
│   └── handlers/
│       ├── start.py           # /start, меню
│       ├── add_property.py    # Добавление ЖК
│       ├── query.py           # Поиск, просмотр ЖК
│       └── kp.py              # Генерация КП с выбором стиля
│
├── services/
│   ├── telegram.py            # Telegram API
│   ├── llm.py                 # OpenAI API
│   ├── parser.py              # Парсинг файлов (PDF, Excel, DOCX, images)
│   ├── content_composer.py    # AI-генерация контента для документов
│   ├── kp_generator_v2.py     # PDF рендеринг с динамическими стилями
│   ├── pdf_styles.py          # 6 стилей оформления
│   ├── style_advisor.py       # AI-выбор стиля (не используется)
│   └── fonts/                 # DejaVuSans для PDF
│
├── db/
│   ├── database.py            # SQLite операции
│   └── models.py              # Dataclass модели
│
└── data/
    ├── assistant.db           # SQLite база
    ├── uploads/               # Загруженные файлы
    └── kp_output/             # Сгенерированные PDF
```

---

## ✅ Реализованный функционал (v0.2)

### Управление ЖК
- Добавление ЖК с автопарсингом файлов
- Просмотр списка ЖК
- Карточка ЖК с документами
- Удаление ЖК

### Парсинг файлов
- PDF (текст через PyMuPDF)
- Excel (pandas)
- DOCX (python-docx)
- Изображения (GPT-4 Vision)
- LLM извлекает структурированные данные

### Генерация КП
- Content Composer — AI создаёт продающий контент
- 6 стилей: premium, business, corporate, modern, minimal, warm
- Выбор стиля кнопками
- Динамический ColorBlock (автоподбор шрифта)

### Поиск
- В контексте ЖК: "что есть до 15 млн?"
- Глобальный по всей базе

---

## 🗄 База данных
```sql
-- users
id, telegram_id, username, first_name, last_name, state, state_data, created_at

-- properties
id, user_id, name, address, developer, completion_date,
price_min, price_max, price_per_sqm_min, price_per_sqm_max,
apartment_types, area_min, area_max,
payment_options, installment_terms, mortgage_info, commission,
description, features, raw_data, created_at, updated_at

-- property_files
id, property_id, user_id, file_id, file_name, file_type, file_path, extracted_text, created_at
```

---

## 🎨 Стили PDF

| Стиль | Описание | Когда использовать |
|-------|----------|-------------------|
| premium | Тёмный, золотые акценты | Элитная недвижимость |
| business | Строгий, зелёные акценты | Для инвесторов |
| corporate | Сдержанный синий | Профессиональный |
| modern | Яркий, оранжевые акценты | Универсальный |
| minimal | Чёрно-белый | Много текста |
| warm | Тёплые тона | Для семей |

---

## 📋 История версий

### v0.2.0 (07.01.2026)
- Content Composer — AI создаёт продающий контент
- 6 стилей PDF с выбором кнопками
- Динамический ColorBlock
- GitHub репозиторий

### v0.1.0 (05.01.2026)
- Базовый бот с FSM
- Добавление/просмотр ЖК
- Парсинг файлов
- Простая генерация КП
