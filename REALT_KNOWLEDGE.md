# Realt Assistant — База знаний

## 🔑 Критически важные знания

### 1. Запуск бота — только через полный путь к python
```bash
# ❌ НЕПРАВИЛЬНО — venv не активируется в фоне
nohup python run_polling.py &

# ✅ ПРАВИЛЬНО — полный путь к интерпретатору
cd /opt/realt-assistant
/opt/realt-assistant/venv/bin/python -u run_polling.py > bot.log 2>&1 &
```

**Почему**: При запуске через `nohup ... &` виртуальное окружение не активируется, используется системный python без нужных библиотек.

### 2. JSON в промптах — не использовать .format()
```python
# ❌ НЕПРАВИЛЬНО — фигурные скобки в JSON конфликтуют с .format()
prompt = """Верни JSON: {"name": "{name}"}""".format(name=value)

# ✅ ПРАВИЛЬНО — использовать .replace()
prompt = """Верни JSON: {"name": "{name}"}"""
prompt = prompt.replace("{name}", value)
```

**Почему**: В промптах с примерами JSON много `{}`, они конфликтуют с Python `.format()`.

### 3. Парсинг JSON от LLM — regex надёжнее
```python
# LLM может вернуть ```json ... ``` или текст вокруг JSON
result_text = response.choices[0].message.content.strip()

# Убираем markdown
result_text = re.sub(r'```json\s*', '', result_text)
result_text = re.sub(r'```\s*', '', result_text)

# Ищем JSON объект
match = re.search(r'\{[\s\S]*\}', result_text)
if match:
    result_text = match.group(0)

data = json.loads(result_text)
```

### 4. Проверка процесса — grep по пути
```bash
# ❌ Поймает другие боты с run_polling.py
ps aux | grep "run_polling"

# ✅ Точный поиск нашего бота
ps aux | grep "realt-assistant" | grep -v grep
```

### 5. Другие сервисы на сервере — НЕ ТРОГАТЬ
```
/opt/bot          — RIZALTA PROD (порт 8000)
/opt/bot-dev      — RIZALTA DEV (порт 8002)
/opt/sofia-claude — Sofia Claude
/opt/sofia-gpt    — Sofia GPT
```

При `pkill` всегда указывать полный путь или паттерн:
```bash
pkill -f "realt-assistant.*run_polling"
```

---

## 🛠 Частые команды
```bash
# === ЗАПУСК/ОСТАНОВКА ===
cd /opt/realt-assistant
/opt/realt-assistant/venv/bin/python -u run_polling.py > bot.log 2>&1 &
pkill -f "realt-assistant.*run_polling"
ps aux | grep "realt-assistant" | grep -v grep

# === ЛОГИ ===
tail -f /opt/realt-assistant/bot.log
tail -50 /opt/realt-assistant/bot.log

# === КЭШИ ===
find /opt/realt-assistant -name "__pycache__" -exec rm -rf {} + 2>/dev/null
rm -rf /opt/realt-assistant/__pycache__ /opt/realt-assistant/*/__pycache__ /opt/realt-assistant/*/*/__pycache__

# === БАЗА ДАННЫХ ===
sqlite3 /opt/realt-assistant/data/assistant.db "SELECT id, name FROM properties;"
sqlite3 /opt/realt-assistant/data/assistant.db "SELECT id, file_name FROM property_files WHERE property_id = 1;"

# === GIT ===
cd /opt/realt-assistant
git add -A
git commit -m "описание"
git push
```

---

## 🐛 Решённые проблемы

### Проблема: "ModuleNotFoundError: No module named 'fitz'"
**Причина**: Запуск без активации venv
**Решение**: Использовать полный путь `/opt/realt-assistant/venv/bin/python`

### Проблема: JSON parse error в Content Composer
**Причина**: `.format()` ломает JSON в промпте
**Решение**: Использовать `.replace()` вместо `.format()`

### Проблема: Текст обрезается в ColorBlock
**Причина**: Фиксированная высота и размер шрифта
**Решение**: Динамический `_calc_font_size()` с уменьшением шрифта

### Проблема: Зомби-процессы [python]
**Причина**: Некорректное завершение процесса
**Решение**: `kill -9 PID` для зомби, правильный pkill для остальных

### Проблема: Бот не отвечает после перезапуска
**Причина**: Процесс не запустился или упал
**Решение**: Проверить `ps aux | grep realt-assistant`, посмотреть `bot.log`

---

## 📐 Архитектурные решения

### Почему Content Composer + Renderer (а не шаблоны)

**Было**: Данные → Шаблон → PDF (тупая подстановка)
**Стало**: Данные → LLM создаёт контент → Renderer → PDF

**Причина**: Риэлторы загружают разную информацию. LLM понимает контекст, убирает мусор, делает продающий текст.

### Почему 6 стилей + выбор кнопками

**Было**: AI сам выбирает стиль
**Стало**: Пользователь выбирает из 6 кнопок

**Причина**: Риэлтор лучше знает своего клиента. AI-выбор оставили как опцию "Авто".

### Почему SQLite (а не PostgreSQL)

**Причина**: Пока один пользователь, простота важнее масштабируемости. Миграция на PostgreSQL в планах.

---

## 📊 Стили PDF — когда какой

| Клиент | Объект | Рекомендуемый стиль |
|--------|--------|---------------------|
| Инвестор | Любой | business, corporate |
| VIP клиент | Премиум ЖК | premium |
| Молодая семья | Комфорт-класс | warm |
| Молодёжь | Студии | modern |
| Деловой человек | Бизнес-класс | corporate, minimal |
| Неизвестно | Любой | modern (универсальный) |

---

## 🔗 Полезные ссылки

- **GitHub**: https://github.com/semiekhin/realt-assistant
- **OpenAI API**: https://platform.openai.com/
- **Telegram Bot API**: https://core.telegram.org/bots/api
- **ReportLab docs**: https://docs.reportlab.com/
